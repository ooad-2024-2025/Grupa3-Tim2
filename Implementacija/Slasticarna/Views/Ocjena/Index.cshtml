@model IEnumerable<Slasticarna.Models.Ocjena>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Narudzba)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Korisnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Narudzba.KorisnikID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Korisnik.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OcjenaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OcjenaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OcjenaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
